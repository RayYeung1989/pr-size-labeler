name: 'PR Size Labeler'

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: Get PR files
        id: files
        uses: actions/github-script@v7
        with:
          script: |
            const { data: files } = await github.rest.pulls.listFiles({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });

            let totalLines = 0;
            for (const file of files) {
              totalLines += file.changes;
            }

            let size = 'M';
            if (totalLines < 10) size = 'XS';
            else if (totalLines < 50) size = 'S';
            else if (totalLines < 200) size = 'M';
            else if (totalLines < 500) size = 'L';
            else size = 'XL';

            core.setOutput('size', size);
            core.setOutput('lines', totalLines);

      - name: Add size label
        if: steps.files.outputs.size != ''
        uses: actions/github-script@v7
        with:
          script: |
            const size = '${{ steps.files.outputs.size }}';
            github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: [`size/${size}`]
            });
